service: infra
frameworkVersion: '3'
configValidationMode: warn
provider:
  name: aws
  deploymentMethod: direct
  region: ap-south-1
  deploymentPrefix: infra

  resources:
    - ${file(./resources/ec2.yml)}
